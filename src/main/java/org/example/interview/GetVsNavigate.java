package org.example.interview;

public class GetVsNavigate {

    /*
    get will take the url
          it wot store the history
          wait until elements appear in DOM

    Navigate
        It will store the history
        back,forward, refresh
        it won't wait

     Maximize Browser

     driver.manage().window().maximize()

     Quit VS Close

     Quit close all the window
     close will close the browser window that the driver has focus off

     Refresh browser

    1) refresh command
    driver.navigate().refresh()
    2) get current url
    driver.get(driver.undercurrent())
    3)Javascript executor
    JavascriptExecutor executor=(JavascriptExecutor)driver;
    executor.executeScript("location.reload()");
    4)Using robot class
    Robot robot=new Robot();
    robot.KeyPress(KeyEvent.VK_F5);
    robot.KeyRelease(KeyEvent.Vk_F5);

    Sleep

    Sleep is no longer available

    How to launch a browser without system.set property

    edit the environmental variable
    add in the path browser location














     */
}
