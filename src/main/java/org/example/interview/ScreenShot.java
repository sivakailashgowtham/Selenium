package org.example.interview;

public class ScreenShot {

    /*

     TakesScreenshot screenshot= (TakesScreenshot) driver;
        File sourceFile=screenshot.getScreenshotAs(OutputType.FILE);
        File file=new File("D://sample.img");
        FileHandler.copy(sourceFile,file);


    Using robot class also we can use

      Robot robot=new Robot();
        Dimension screenSize=Toolkit.getDefaultToolkit().getScreenSize();
        Rectangle rectangle=new Rectangle(screenSize);
       BufferedImage source= robot.createScreenCapture(rectangle);
       File DestinationFile=new File("D://test.png");
        ImageIO.write(source,"png",DestinationFile);



  Scroll up and down
     JavascriptExecutor executor=(JavascriptExecutor)driver;
        executor.executeScript("window.scroll(0,450)","");
        Thread.sleep(2000);
        executor.executeScript("window.scroll(0,-450)","");

    Scrolling to bottom of a page
    window.scrollTo(0,document.body.scrollHeight)
     JavascriptExecutor executor=(JavascriptExecutor)driver;
    executor.executeScript("window.scrollTo(0,document.body.scrollHeight)","");

    Scrolling to Top of a Page
    window.scrollTo(0,0)


    How to handle pop ups

    we cant inspect and sendkeys

    we can use 3rd part tools like sikuli,AutoIt
    send username and password via url


    Actions class


    enable us to handle different mouse and keyboard events

    click()
    sendKeys()
    KeyDown()pressing and holding
    KeyUP()release that key
    DragAndDrop()
    build()
    perform()




     */
}
